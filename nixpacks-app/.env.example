# PostgreSQL Database Connection
# IMPORTANT: When using Coolify, DATABASE_URL comes with 'localhost' but containers need the actual hostname

# For Coolify deployments, add this environment variable:
# DATABASE_INTERNAL_HOST=<database-container-name>
# Example: DATABASE_INTERNAL_HOST=kcwk4kgk8s0ows40ok4o4s08

# Option 1: Connection string (provided automatically by Coolify)
# DATABASE_URL will be set automatically when linked to a Coolify database
# but you may need to add DATABASE_INTERNAL_HOST to fix localhost issues

# Option 2: Manual individual variables (for local development)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=myapp
# DB_USER=postgres
# DB_PASSWORD=secret
# DB_SSL=false

# Server
PORT=3000
